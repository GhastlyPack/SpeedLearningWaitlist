# Customer.io JS snippet config
# Public — safe to expose to the browser. Found in CIO Workspace Settings → API Credentials.
NEXT_PUBLIC_CIO_SITE_ID=

# CIO region: "us" (default) or "eu"
NEXT_PUBLIC_CIO_REGION=us
